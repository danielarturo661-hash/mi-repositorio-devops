version: 0.2
phases:
  install:
    runtime-versions:
      python: 3.9
  build:
    commands:
      - echo "Ejecutando pruebas"
      # - pytest tests/  <-- Comenta esto si aún no tienes tests creados
  post_build:
    commands:
      - echo "Construcción finalizada con éxito"
artifacts:
  files:
    - '**/*'